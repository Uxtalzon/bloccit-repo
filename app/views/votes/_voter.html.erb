<% if policy(Vote.new).create? %>
<div class="pull-left"> <div class="vote-arrows">
  <div>
    <%= link_to " ",
    topic_post_up_vote_path(post.topic, post), 
    class: "glyphicon glyphicon-chevron-up #{(current_user.voted(post) && current_user.voted(post).up_vote?) ? 'voted' : '' }", method: :post %>
  </div>
   <div>
     <strong><%= post.points %></strong>
  </div>
  <div>
    <%= link_to " ",
    topic_post_down_vote_path(post.topic, post), 
    class: "glyphicon glyphicon-chevron-down #{(current_user.voted(post) && cuurent_user.voted(post).down_vote?) ? 'voted' : '' }", method: :post %>
  </div>
</div>
<% end %>